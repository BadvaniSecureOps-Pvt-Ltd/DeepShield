name: deepshield
description: DeepShield - mobile-first deepfake detection
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  http: ^1.1.0
  image_picker: ^1.0.7
  flutter_image_compress: ^2.1.0
  path_provider: ^2.0.15
  path: ^1.8.3
  shared_preferences: ^2.1.1
  uuid: ^4.5.1
  flutter_dotenv: ^6.0.0

  # --- NEW FOR AUTH + GOOGLE SIGN-IN ---
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  google_sign_in: ^5.4.2

  # --- FUTURE-PROOF FOR PREMIUM FLOW ---
  in_app_purchase: ^3.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

flutter:
  assets:
    - .env

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
